name: Jekyll site CI

on:
  push:
    branches: [ "v3-dev" ]
  pull_request:
    branches: [ "v3-dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the site in the jekyll/builder container
      run: |
        docker run \
        -v ${{ github.workspace }}:/srv/jekyll -v ${{ github.workspace }}/docs:/srv/jekyll/_site \
        jekyll/builder:3.8.6 /bin/bash -c "chmod -R 777 /srv/jekyll && jekyll build --future"
